<div class="span-18">
  <ul class="horizontal">
    <% %w{linkedin facebook google github}.each do |service| %>
      <li>
        <%= link_to image_tag("#{service}_128.png"), t(".#{service}.url") %><br>
        <%= content_tag :h4, t(".#{service}.text"), :class => "center quiet" %>
      </li>
    <% end %>
  </ul>
</div>
