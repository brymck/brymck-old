#posts.span-17.colborder
  = render :partial => "post", :locals => { :post => @post, :show_comments => true }
.archive.delink-current{:"data-pjax" => "#posts"}
  = render :partial => "archive", :locals => { :personal => @post.personal }
