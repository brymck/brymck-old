<h2><%=t ".archive" %></h2>
<ul>
  <% Post.where(:personal => defined?(personal) ? personal : false).reverse.each do |post| %>
    <%= content_tag :li, link_to_unless_current(post.title, post) %>
  <% end %>
</ul>
