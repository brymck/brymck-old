<p><%=t :choose, :scope => :sessions %></p>
<ul class="horizontal">
  <% %w{google facebook twitter}.each do |provider| # Add support for Yahoo!, AOL, Github %>
    <li>
      <%= link_to image_tag("#{provider}_128.png"), "/auth/#{provider}" %>
      <%= content_tag :h4, t(".#{provider}"), :class => "center quiet" %>
    </li>
  <% end %>
</ul>
