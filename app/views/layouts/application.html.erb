<!doctype html>
<html>
  <%= render :partial => "layouts/head" %>
  <body>
    <div class="container">
      <h1><%= link_to t(:brymck), home_path %></h1>
      <hr>
      <%= render :partial => "layouts/sidebar" %>
      <hr>
      <%= render_flash(flash) %>
      <%= yield %>
      <%= render :partial => "layouts/footer" %>
    </div>
  </body>
</html>
